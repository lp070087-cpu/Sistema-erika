/**
 * ARQUIVO APAGADO — MANTIDO VAZIO SÓ PARA NÃO DEIXAR RESTO.
 *
 * Os dados dos cards da Central de Planilhas moraram aqui por alguns minutos,
 * duplicando a lista de modelos que já existe em `src/lib/planilhas/gerador.ts`.
 *
 * A duplicação foi desfeita: a lista virou `src/lib/planilhas/modelos.ts`, um
 * módulo PURO que os dois lados leem — o gerador, para saber o que consegue
 * produzir, e a tela, para desenhar os cards. Uma fonte só, sem arrastar o
 * exceljs para o navegador.
 *
 * Este arquivo pode ser apagado. Ele não exporta nada e nada o importa.
 */

export {};
